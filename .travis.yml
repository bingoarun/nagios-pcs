---
python:
  - "2.6"
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
python: "2.7"

before_install:
  # Make sure everything's up to date.
  #- sudo apt-get update -qq

install:
  # Install Ansible.
  - pip install pep8
  
script:
  # Check pep8 errors
  - "pep8 --show-source --show-pep8 check_pcs.py"

